---
import { type Lang, getTranslation } from '@i18n/utils';
import AffiliateDisclosure from '@components/affiliate/AffiliateDisclosure.astro';
import siteConfig from '@data/site-config.json';

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;
const t = getTranslation(lang);
const currentYear = new Date().getFullYear();

const footerLinks = [
  { href: `/${lang}/about`, label: t('footer.about') },
  { href: `/${lang}/privacy-policy`, label: t('footer.privacy') },
  { href: `/${lang}/terms-of-service`, label: t('footer.terms') },
];

const categories = [
  { href: `/${lang}/categories/electronics`, labelEs: 'Electronica', labelEn: 'Electronics' },
  { href: `/${lang}/categories/home-kitchen`, labelEs: 'Hogar y Cocina', labelEn: 'Home & Kitchen' },
  { href: `/${lang}/categories/gaming`, labelEs: 'Gaming', labelEn: 'Gaming' },
];
---

<footer class="footer">
  <div class="footer-inner">
    <!-- Main Footer Grid -->
    <div class="footer-grid">
      <!-- Brand Column -->
      <div class="footer-brand">
        <a href={`/${lang}`} class="brand-link">
          <span class="brand-mark">
            <svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
              <path d="M12 2L2 7l10 5 10-5-10-5z" fill="currentColor" opacity="0.9"/>
              <path d="M2 17l10 5 10-5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              <path d="M2 12l10 5 10-5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </span>
          <span class="brand-name">{siteConfig.site.name}</span>
        </a>
        <p class="brand-tagline">
          {siteConfig.site.tagline[lang]}
        </p>
      </div>

      <!-- Quick Links -->
      <div class="footer-column">
        <h4 class="column-title">
          {lang === 'es' ? 'Enlaces' : 'Links'}
        </h4>
        <ul class="column-list">
          {footerLinks.map((link) => (
            <li>
              <a href={link.href} class="column-link">
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Categories -->
      <div class="footer-column">
        <h4 class="column-title">
          {lang === 'es' ? 'Categorias' : 'Categories'}
        </h4>
        <ul class="column-list">
          {categories.map((cat) => (
            <li>
              <a href={cat.href} class="column-link">
                {lang === 'es' ? cat.labelEs : cat.labelEn}
              </a>
            </li>
          ))}
        </ul>
      </div>

    </div>

    <!-- Affiliate Disclosure -->
    <div class="footer-disclosure">
      <AffiliateDisclosure lang={lang} variant="footer" />
    </div>

    <!-- Bottom Bar -->
    <div class="footer-bottom">
      <p class="copyright">
        &copy; {currentYear} {siteConfig.site.name}. {lang === 'es' ? 'Todos los derechos reservados.' : 'All rights reserved.'}
      </p>
      <div class="footer-meta">
        <span class="meta-item">Amazon {lang === 'es' ? 'Afiliado' : 'Affiliate'}</span>
        <span class="meta-divider">·</span>
        <span class="meta-item">{lang === 'es' ? 'Hecho con cuidado' : 'Made with care'}</span>
      </div>
    </div>
  </div>
</footer>

<style>
  /* Neo-Brutalist Footer */
  .footer {
    background: var(--color-brutal-black, #1A1A1A);
    color: var(--color-cream, #FFFEF9);
    border-top: 4px solid var(--color-brutal-yellow, #FFE14D);
  }

  .footer-inner {
    max-width: 1200px;
    margin: 0 auto;
    padding: 4rem 1.5rem 2rem;
  }

  /* Grid */
  .footer-grid {
    display: grid;
    grid-template-columns: 1.5fr repeat(2, 1fr);
    gap: 3rem;
    margin-bottom: 3rem;
  }

  /* Brand */
  .footer-brand {
    padding-right: 2rem;
  }

  .brand-link {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    text-decoration: none;
    color: inherit;
    margin-bottom: 1rem;
    transition: transform 150ms cubic-bezier(0.68, -0.6, 0.32, 1.6);
  }

  .brand-link:hover {
    transform: translate(-2px, -2px);
  }

  .brand-mark {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 2.5rem;
    height: 2.5rem;
    color: var(--color-brutal-black, #1A1A1A);
    background: var(--color-brutal-yellow, #FFE14D);
    border: 2px solid var(--color-brutal-yellow, #FFE14D);
  }

  .brand-mark svg {
    width: 70%;
    height: 70%;
  }

  .brand-name {
    font-family: var(--font-display, 'Space Grotesk', sans-serif);
    font-size: 1.5rem;
    font-weight: 700;
    letter-spacing: -0.02em;
    text-transform: uppercase;
  }

  .brand-tagline {
    font-family: var(--font-body, 'DM Sans', sans-serif);
    font-size: 0.9375rem;
    color: rgba(255, 254, 249, 0.6);
    line-height: 1.6;
    margin: 0 0 1.25rem;
  }

  /* Columns */
  .footer-column {
    /* Column styles */
  }

  .column-title {
    font-family: var(--font-mono, 'JetBrains Mono', monospace);
    font-size: 0.6875rem;
    font-weight: 700;
    color: var(--color-brutal-yellow, #FFE14D);
    text-transform: uppercase;
    letter-spacing: 0.15em;
    margin: 0 0 1.25rem;
    padding-bottom: 0.5rem;
    border-bottom: 2px solid var(--color-brutal-yellow, #FFE14D);
    display: inline-block;
  }

  .column-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .column-link {
    font-family: var(--font-body, 'DM Sans', sans-serif);
    font-size: 0.9375rem;
    color: rgba(255, 254, 249, 0.7);
    text-decoration: none;
    transition: all 150ms cubic-bezier(0.68, -0.6, 0.32, 1.6);
    position: relative;
    display: inline-block;
  }

  .column-link::after {
    content: '';
    position: absolute;
    bottom: -2px;
    left: 0;
    width: 0;
    height: 2px;
    background: var(--color-brutal-yellow, #FFE14D);
    transition: width 150ms cubic-bezier(0.68, -0.6, 0.32, 1.6);
  }

  .column-link:hover {
    color: var(--color-cream, #FFFEF9);
    transform: translateX(4px);
  }

  .column-link:hover::after {
    width: 100%;
  }

  /* Disclosure */
  .footer-disclosure {
    padding: 1.5rem 0;
    border-top: 3px solid rgba(255, 225, 77, 0.3);
    border-bottom: 3px solid rgba(255, 225, 77, 0.3);
    margin-bottom: 1.5rem;
  }

  .footer-disclosure :global(p) {
    font-family: var(--font-mono, 'JetBrains Mono', monospace);
    font-size: 0.75rem;
    color: rgba(255, 254, 249, 0.5);
    line-height: 1.7;
    margin: 0;
  }

  /* Bottom */
  .footer-bottom {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    padding-top: 0.5rem;
  }

  .copyright {
    font-family: var(--font-mono, 'JetBrains Mono', monospace);
    font-size: 0.75rem;
    color: rgba(255, 254, 249, 0.5);
    margin: 0;
  }

  .footer-meta {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    font-family: var(--font-mono, 'JetBrains Mono', monospace);
    font-size: 0.75rem;
    color: rgba(255, 254, 249, 0.5);
  }

  .meta-item {
    padding: 0.25rem 0.5rem;
    background: rgba(255, 225, 77, 0.1);
    border: 1px solid rgba(255, 225, 77, 0.2);
  }

  .meta-divider {
    color: var(--color-brutal-yellow, #FFE14D);
    font-weight: 700;
  }

  /* Responsive */
  @media (max-width: 900px) {
    .footer-grid {
      grid-template-columns: 1fr 1fr;
      gap: 2.5rem;
    }

    .footer-brand {
      grid-column: span 2;
      padding-right: 0;
    }
  }

  @media (max-width: 640px) {
    .footer {
      border-top-width: 3px;
    }

    .footer-inner {
      padding: 3rem 1rem 1.5rem;
    }

    .footer-grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .footer-brand {
      grid-column: span 1;
    }

    .footer-bottom {
      flex-direction: column;
      text-align: center;
      gap: 1rem;
    }

    .footer-meta {
      flex-wrap: wrap;
      justify-content: center;
    }
  }
</style>
